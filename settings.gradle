include ':app', ':pressurego', ':pressurego_example'
rootProject.name = "PressureGoApp"

if (hasProperty('USE_LOCAL_ALGORIGO_BLE')) {
    include ':algorigoble2'
    project(':algorigoble2').projectDir = new File(ALGORIGO_BLE_2_PATH + "/algorigoble2")
}